TODO:

- add all configurable values to ini to customize (colors, fonts etc)

- launcher related settings to apply on save instead of restart

- hamburguer menu for advanced options, like all vpxtool args, see logs etc
